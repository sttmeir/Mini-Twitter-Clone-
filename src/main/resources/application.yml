spring:
  datasource:
    url: jdbc:h2:mem:testdb # jdbc:h2:file:~/testdb - this way it will store on disk
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: asncnJNAJKbjkd518d48eSafsef5851dfrg473mslasdef785146efdg
  lifetime: 30m
server:
  port: 8080

h2:
  console:
    enabled: true
    path: /h2-console

logging:
  level:
    org.springframework.security: DEBUG
