spring:
  datasource:
    url: jdbc:postgresql://db:5432/nottify
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: ideappad147
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
jwt:
  secret: asncnJNAJKbjkd518d48eSafsef5851dfrg473mslasdef785146efdg
  lifetime: 30m
server:
  port: 8080

logging:
  level:
    org.springframework.security: DEBUG

postgresql:
  init-mode: always

